/**
 * Guild Intent Mapping (Re-export)
 * Moved to intents/guild.ts for better organization.
 * Use TypeScript source path to avoid .js resolution issues in test environment.
 */

export { guildIntentMap, resolveGuildIntent } from '../intents/guild.js';
